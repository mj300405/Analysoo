build:
  docker:
    web: ./backend/Dockerfile.prod

run:
  web:
    image: web
    command:
      - gunicorn --chdir backend BreakWordTrapsBackend.wsgi:application --bind 0.0.0.0:$PORT
  worker:
    image: web
    command:
      - celery -A BreakWordTrapsBackend worker -l info
