setup:
  addons:
    - plan: heroku-postgresql
    - plan: heroku-redis

build:
  docker:
    web: backend/Dockerfile.prod

run:
  web:
    command:
      - gunicorn BreakWordTrapsBackend.wsgi:application --bind 0.0.0.0:$PORT
    image: web
  worker:
    command:
      - celery -A BreakWordTrapsBackend worker -l info
    image: web